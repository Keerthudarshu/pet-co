## Test configuration - use local MySQL
## This file forces tests to use the same MySQL instance used at runtime.
## Make sure a MySQL instance is running (see docker-compose.yml in the project root).

spring.datasource.url=jdbc:mysql://localhost:3306/pet_co?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Nishu@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Keep JPA behavior consistent for tests
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Hikari pool settings for tests
spring.datasource.hikari.maximum-pool-size=5

## If you want tests to run against a different DB or credentials, override these properties
## with environment variables or a CI-specific properties file.

